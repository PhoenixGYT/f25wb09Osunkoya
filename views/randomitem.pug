extends layout

block content
  h1 A Random Item

  -
    const image_names = ['tat1', 'tat2', 'tat3', 'tat4', 'tat5'];

  -
    for (let i = 0; i < 3; i++) {
      const index = Math.floor(Math.random() * 5);
      const name = image_names[index];
  img(src=`/images/${name}.png`, alt=`Image of ${name}`, height="300", width="300")
  - }
